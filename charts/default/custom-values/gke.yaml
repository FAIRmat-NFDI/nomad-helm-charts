# GKE (Google Kubernetes Engine) deployment values for NOMAD Oasis.
#
# Target: a GKE Autopilot cluster. Uses the built-in GCE ingress (Google Cloud
# HTTP Load Balancer) with a pre-reserved global static IP, and Filestore
# (ReadWriteMany) for the NOMAD data volumes shared by the app and worker pods.
#
# Prerequisites (full details in custom-values/README.md):
#   1. An Autopilot cluster with kubectl configured.
#   2. A global static IP:  gcloud compute addresses create nomad-ip --global
#   3. Filestore CSI driver (enabled by default on Autopilot) AND the Cloud Filestore
#      API (for the ReadWriteMany volumes): gcloud services enable file.googleapis.com
#
# This file is HTTP-only. See the "Enabling HTTPS" section of the README to add
# a Google-managed certificate.

nomad:
  config:
    services:
      # Must be a DNS name -- a Kubernetes Ingress rejects a bare IP in its host
      # rule. Use a domain whose DNS A-record points to the static IP, or, for a
      # quick test without a domain, an nip.io name that resolves to the IP, e.g.
      # <IP>.nip.io (the static IP with ".nip.io" appended).
      api_host: <DOMAIN>
      api_base_path: /nomad-oasis
      https: false   # set true after enabling HTTPS (see README)

  # Shared NOMAD data volumes (public, staging, tmp) are mounted concurrently by
  # the app and worker pods, so they require ReadWriteMany -> Filestore.
  persistence:
    enabled: true
    storageClass: standard-rwx     # Filestore (RWX). See README for a cheaper multishare option.
    accessMode: ReadWriteMany
    # north-home is only mounted by the app pod -> ReadWriteOnce Persistent Disk is enough.
    north-home:
      storageClass: standard-rwo
      accessMode: ReadWriteOnce

  # Autogenerated API secret
  secrets:
    api:
      existingSecret: ""
      key: password
      value: ""
      autoGenerate: true

  # GCE ingress = Google Cloud HTTP Load Balancer.
  # Reserve the IP first:  gcloud compute addresses create nomad-oasis-ip --global
  ingress:
    enabled: true
    className: "gce"
    annotations:
      # GKE's ingress controller (glbc) selects ingresses via THIS annotation and
      # ignores spec.ingressClassName. The chart only emits the annotation on k8s
      # <1.18, so on modern clusters glbc never claims the ingress and no load
      # balancer is ever created -- set it explicitly here. (className stays "gce"
      # so the secondary ingress-api.yaml template remains disabled.)
      kubernetes.io/ingress.class: "gce"
      kubernetes.io/ingress.global-static-ip-name: "nomad-oasis-ip"
    tls: []

  # The proxy's default readiness probe sets timeoutSeconds (63) > periodSeconds (3).
  # Plain kubelet tolerates that, but the GCE load balancer derives its backend health
  # check from this probe and rejects it ("Error 400: TimeoutSec should be less than
  # checkIntervalSec"), which blocks the whole LB. Give it a probe where timeout < period.
  proxy:
    readinessProbe:
      initialDelaySeconds: 90
      periodSeconds: 10
      timeoutSeconds: 5

# Databases use ReadWriteOnce Persistent Disk.
mongodb:
  auth:
    enabled: true
    rootPassword: "changeme"   # change to a secure password
  persistence:
    storageClass: standard-rwo

elasticsearch:
  clusterHealthCheckParams: "wait_for_status=yellow&timeout=1s"
  # Autopilot forbids privileged containers, so the init container that raises
  # vm.max_map_count cannot run. Disable it and switch Elasticsearch off mmapfs
  # so it does not trip the vm.max_map_count bootstrap check.
  sysctlInitContainer:
    enabled: false
  esConfig:
    elasticsearch.yml: |
      node.store.allow_mmap: false
  persistence:
    enabled: true
    storageClass: standard-rwo
  volumeClaimTemplate:
    storageClassName: standard-rwo
    resources:
      requests:
        storage: 20Gi

postgresql:
  primary:
    persistence:
      storageClass: standard-rwo

# The Temporal subchart declares no resource requests, so GKE Autopilot stamps its
# default 500m/2Gi onto every Temporal pod (frontend, history, matching, worker, web,
# admintools) -- ~12Gi of requests for an idle demo stack, which fills the nodes and
# forces Autopilot to add a node (and a ~100Gi SSD boot disk) the app/worker don't need.
# Set lean explicit requests so the whole stack fits on a small cluster. Tune upward for
# production load (history is the most memory-sensitive server role).
temporal:
  server:        # frontend, history, matching, worker
    resources:
      requests:
        cpu: 100m
        memory: 512Mi
      limits:
        cpu: 500m
        memory: 1Gi
  web:
    resources:
      requests:
        cpu: 50m
        memory: 128Mi
      limits:
        cpu: 250m
        memory: 256Mi
  admintools:
    resources:
      requests:
        cpu: 50m
        memory: 128Mi
      limits:
        cpu: 250m
        memory: 256Mi
